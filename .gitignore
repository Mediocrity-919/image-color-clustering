# Dependencies
node_modules/

# Local npm cache (created because the default npm cache dir was not writable)
.npm-cache/

# Logs
*.log

# OS files
.DS_Store

# Secrets — never commit API keys or proxy env files
api.txt
.dev.vars
.env
.env.*
proxy/.dev.vars
